@model IEnumerable<InmuebleVenta.Entities.Factura>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Monto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContratoResarvaId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ContratoAlquilerId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Monto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContratoResarvaId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ContratoAlquilerId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComprobanteId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComprobanteId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComprobanteId })
        </td>
    </tr>
}

</table>
